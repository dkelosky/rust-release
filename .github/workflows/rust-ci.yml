name: Rust CI

on:
  push:
  pull_request:

jobs:
  build:

    # runs-on: ${{ matrix.os }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@v2

    - name: Build
      run: cargo build --verbose --release

    - name: Run tests
      run: cargo test --verbose

    - name: Get version
      run: echo '::set-output name=ZOWE2_VERSION::green'
      # run: ":::set-output name=ZOWE2_VERSION::cargo pkgid | cut -d# -f2 | cut -d: -f2"
      id: get-version

    - name: Get color
      run: echo "The selected color is ${{ steps.get-version.outputs.ZOWE2_VERSION }}"

    # - name: Zip
    #   run: zip --junk-paths zowe2 target/release

    # - name: Create Release
    #   id: create_release
    #   uses: actions/create-release@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
    #   with:
    #     tag_name: ${{ steps.get-version.outputs.ZOWE2_VERSION }}
    #     release_name: Release ${{ steps.get-version.outputs.ZOWE2_VERSION }}
    #     body: |
    #       Changes in this Release
    #       - First Change
    #       - Second Change
    #     draft: false
    #     prerelease: false

    # - name: Upload Release Asset
    #   id: upload-release-asset
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
    #     asset_path: ./zowe2.zip
    #     asset_name: ${{ matrix.os }}-zowe2.zip
    #     asset_content_type: application/zip